# 진행 상황

## 현재 상태 (2026-09-10)

**조사 완료, 방향 결정 대기.** 프로바이더 스파이크 1건과 조사 3건(국내 프로바이더, 국제
오픈소스, 국내 시장)을 마쳤다. 설계에 착수하지 않았다. 코드는 스파이크 하네스뿐이다.

프로젝트 발단: speech-toolkit(배치 TTS·STT CLI 모음)에 실시간 전사를 넣을지 논의하다가,
입출력 계약·의존성·트리거 표면이 모두 달라 분리하기로 했고, 이어서 목표가 CLI 스크립트가
아니라 청각장애인용 웹앱/iOS 앱임이 분명해져 별도 프로젝트로 떼어 냈다.

## 프로젝트 전제 (2026-09-05 확정)

**상용 제품과 경쟁하지 않는다.** 소보로 플러스·씨사운드는 이미 존재하고, 씨사운드는 8인 회의를
따라갈 만큼의 실시간 화자 분리 품질을 실제로 낸다. 기능으로 이기려는 시도는 목표가 아니다.

**목표는 진입 장벽 제거다.** 보조공학기기와 소프트웨어는 비싸고(씨사운드 기기 170만 원 + 인앱
구독, 소보로 월 9,900원) 공공지원 경로에 묶여 있다. **누구나 그냥 내려받아 쓰는 무료 미니멀
앱이 없다.** 그 자리를 채우고 **오픈소스로 공개해 생태계를 넓힌다** — 길동무(스크린리더
사용자용 내비게이션)와 같은 전략이다.

따라서 성공 판정 기준은 점유율이나 정확도 1위가 아니라 다음 셋이다.

1. 설치 후 계정·결제·설정 없이 **첫 캡션까지 도달**한다.
2. 사용자가 **한 푼도 내지 않는다**(구독 없음, 키 발급 없음이 이상적).
3. 코드가 공개되어 **다른 개발자가 포크·개조**할 수 있다.

### 온디바이스는 프라이버시 기능이 아니라 무료의 전제다 (근거 재배치)

앞선 PROGRESS는 온디바이스를 "프라이버시라는 차별화 축"으로 적었다. **순서가 틀렸다.**
진짜 이유는 비용이다. 라이브 캡션은 오디오를 **연속으로** 흘려보내는, API 과금상 가장 비싼
사용 형태다(계산은 아래). 무료 배포와 클라우드 STT는 산술적으로 양립하지 않는다.

| 방식 | 1시간 | 1인 월 20시간 | 사용자 100명 |
|---|---|---|---|
| 리턴제로(시간당 1,000원) | 1,000원 | 20,000원 | **월 200만 원** |
| Deepgram 스트리밍(분당 $0.0077 가정, 1,400원/달러) | 647원 | 12,936원 | **월 129만 원** |

소보로 구독료 9,900원으로 감당되는 양은 **월 10~20시간**뿐이다. 즉 기존 서비스의 구독료는
이윤이 아니라 대체로 **원가**다. 우리가 무료로 가려면 그 원가를 0으로 만드는 수밖에 없고,
**온디바이스가 그 유일한 방법**이다. 프라이버시(병원·상담·법률 대화가 밖으로 안 나감)는
그렇게 택한 구조의 **부수 효과**이지 출발점이 아니다.

### 길동무 모델은 그대로 옮겨오지 못한다

길동무는 무료 앱이지만 서버(Vercel API Routes)에서 **위원장 본인 키**로 네이버·ODsay·
Deepgram을 호출한다. 길찾기는 요청이 **띄엄띄엄** 발생해 그 부담이 성립한다. 라이브 캡션은
**켜 놓은 시간 내내** 과금되므로 같은 구조를 쓰면 사용자가 늘수록 위원장이 비용을 떠안는다.
"무료 배포"라는 목표가 곧바로 아키텍처를 구속한다.

### 온디바이스 범위: 하이브리드로 확정 (2026-09-05 위원장 판정)

**기본은 온디바이스 무료, 고급 엔진은 사용자 본인 키로 선택.**

- **기본 경로(정본)**: 설치 → 마이크 허용 → 자막. 계정·결제·설정·네트워크 없음.
  이 경로만으로 앱이 완결되어야 한다. 키를 넣지 않은 사용자가 "반쪽짜리를 쓰고 있다"고
  느끼면 이 결정은 실패한 것이다.
- **선택 경로**: 정확도나 화자 분리가 더 필요한 사용자가 설정에서 외부 STT 키를 넣으면
  그 엔진으로 올라간다. **사용료는 사용자가 해당 사업자에게 직접 낸다** — 우리는 중개하지
  않고 수취하지 않는다. 서버를 두지 않는다는 원칙은 여기서도 유지된다.

**이 구조가 오픈소스 전략과 맞물린다.** 엔진을 갈아 끼울 수 있는 경계가 처음부터 존재하므로,
포크한 개발자가 리턴제로·Deepgram·자체 서버를 붙이는 비용이 낮다. 생태계 확장이 목표라면
단일 엔진 고정보다 이쪽이 낫다.

**설계 제약 (미니멀 원칙과의 긴장을 여기서 못 박는다)**

1. **옵션은 하나다.** 설정에 "고급 엔진" 한 항목만 둔다. 엔진별 세부 손잡이(모델 선택·
   품질 단계·언어 옵션)를 늘리지 않는다. 키를 넣지 않은 사용자는 그 항목의 존재를 몰라도
   앱을 100% 쓸 수 있어야 한다.
2. **3-state를 뭉개지 않는다.** "기기 안에서 동작 중" / "고급 엔진으로 동작 중" /
   "고급 엔진이 실패해 기기 안으로 되돌아옴"은 서로 다른 상태다. 특히 세 번째를 침묵으로
   처리하면 안 된다 — 사용자는 자기 대화가 어디로 가는지 알 권리가 있고, 정확도가 갑자기
   떨어진 이유도 그것이다. **최종 사용자가 청각장애인이므로 이 신호는 소리가 아니라
   화면으로 전달한다.**
3. **`SFSpeechRecognizer`의 무음 서버 폴백과 겹친다.** 기본 경로가 조용히 애플 서버로
   넘어가면 "온디바이스 기본"이라는 약속 자체가 거짓이 된다. 어느 경로로 도는지는 문서가
   아니라 실측으로 확정하고, 확정 못 하면 그 API를 쓰지 않는다.

### 지원 플랫폼: iOS 네이티브 + 웹앱 (2026-09-05 위원장 판정)

**iOS 앱과 웹앱만 만든다. 웹앱은 모바일 브라우저를 지원한다. 안드로이드 네이티브 앱은
장기 과제로 미룬다.**

### ⚠ 웹에서는 "무료"와 "온디바이스"가 갈라진다 (2026-09-05 실측)

Chrome 152(macOS)에서 Web Speech API를 직접 호출해 확인했다. 추측이 아니라 측정값이다.

| 실행 환경 | 무료인가 | 온디바이스인가 | 근거 |
|---|---|---|---|
| **iOS 네이티브 앱** | O | **O** | `SpeechAnalyzer`, 네트워크 호출 없음 |
| **데스크톱 Chrome 웹** | O | **O (팩 설치 후)** | `available({langs:['ko-KR'], processLocally:true})` → **`downloadable`** |
| **iOS Safari 웹(=아이폰 모든 브라우저)** | O | **X** | Safari는 오디오를 **애플 서버로 전송**하며 그 사실을 모달로 고지한다 |
| 안드로이드 Chrome 웹 | O | 미확인 | 팩 가용성은 브라우저·기기 의존 |

측정 원문: `ko-KR` + `processLocally:true` → `"downloadable"`(한국어 온디바이스 팩이
**존재하고 내려받을 수 있다**), `processLocally:false` → `"available"`. `en-US`도 똑같이
`downloadable`이라 한국어만의 공백이 아니라 이 기기에 팩이 아직 없을 뿐이다.

**함의 셋**

1. **아이폰 웹앱은 온디바이스가 아니다.** iOS의 모든 브라우저는 WebKit이므로 크롬을 깔아도
   같다. 사용료는 안 들지만 대화가 애플 서버로 나간다. **"기기 안에서만 돕니다"를 웹앱
   화면에 무조건 띄우면 거짓말이 된다.**
2. **역할이 갈린다.** iOS 네이티브 앱이 완전판(무료 + 온디바이스)이고, 웹앱은 **설치 없이
   일단 써 보는 입구**다. 웹앱을 네이티브의 열등한 복제로 만들지 않는다 — 설치가 필요 없다는
   것이 웹앱만의 값이다.
3. **앞서 확정한 3-state 불변식이 웹에서 그대로 쓰인다.** `available()`이 돌려주는
   `available` / `downloadable` / `downloading` / `unavailable`이 우리가 화면에 표시해야 할
   상태와 정확히 대응한다. 이 값을 그대로 신호로 삼는다.

### 프로토타입은 웹앱 먼저 (2026-09-06 위원장 판정)

**첫 프로토타입은 웹앱으로 만든다. 근거는 URL 하나로 바로 공유해 볼 수 있다는 것이다.**

앞선 문서는 "프라이버시·비용 축을 택하면 iOS가 먼저"라고 적었으나, 위원장은 **무엇을 먼저
만들지가 아니라 무엇을 먼저 검증할지**를 기준으로 삼았다. 이 프로젝트에서 판정을 여는 재료는
전부 당사자 증언이었고(티로 조사에서 재확인), 공유 가능한 URL이 그 재료를 만드는 가장 빠른
수단이다. TestFlight는 설치·초대·심사가 끼어 이 루프를 며칠 단위로 늦춘다.

**프로토타입이 검증하는 것**: 가독성(글자 크기·대비), 읽던 자리를 잃지 않는 스크롤, 확정
텍스트와 중간 결과의 시각적 구분, 켜고 끄는 워크플로, 그리고 **당사자가 이걸 쓰고 싶어하는가**.

**프로토타입이 검증하지 못하는 것**: 온디바이스 여부, 그에 딸린 비용 구조와 프라이버시 약속,
최종 정확도(엔진이 다르다). ⚠ **아이폰에서 열면 오디오는 애플 서버로 나간다.** 이건 웹 경로의
성질이지 프로토타입이라서 생기는 임시 조건이 아니다. 따라서 둘을 지킨다.

1. 프로토타입 화면에 "기기 안에서만 처리"를 쓰지 않는다.
2. **당사자에게 테스트를 부탁할 때 이 사실을 먼저 알린다.** 병원·상담 같은 민감한 자리에서
   시험해 볼 수 있는 도구를 건네는 것이므로, 고지 없이 넘기지 않는다.

배포는 정적 호스팅으로 충분하다(서버 0). "서버를 두지 않는다"는 원칙과 충돌하지 않는다.

### 다음에 확인할 것 (스파이크)

- **모바일 Safari의 재시작 경계에서 발화가 보존되는가.** iPhone 13 Pro의 Safari 26.6.1에서
  한 번은 약 40초 간격으로 `audio-capture: Source is stopped`와
  `aborted: No speech detected`가 발생하며 103초 동안 인식이 두 번 끊겼다. 즉시 재시작은 각각
  4ms와 2ms 안에 `audiostart`까지 갔다. 같은 상태에서 487초간 다시 측정하자 약 40.2초 간격으로
  12번 끊겼고, 첫 재시작 뒤 한 발화도 감지하지 못했다. `audiostart`만으로 실제 마이크 입력의
  복구를 판정할 수 없다는 뜻이다.

  그러나 새로 연 페이지에서는 첫 인식 세션이 163초간 끊기지 않았고, 156초 뒤 시작한 발화를
  확정 결과로 반환한 뒤 사용자 정지로 정상 종료했다. 오류와 자동 재시작은 없었다. 따라서
  40초 종료는 침묵에 따른 고정 상한이 아니라 WebKit 오디오 캡처 상태에 따라 달라지는 간헐적
  실패로 본다. 재시작을 5초 늦추는 실험 버전을 만들었지만 성공한 163초 세션에서는 재시작 자체가
  발생하지 않아 우회 효과는 아직 미검증이다. 다음 실측에서는 오류가 발생한 뒤 5초 재시작을
  거친 세션에서 발화가 보존되는지 확인한다.

  사용자 정지 뒤 새 세션이 시작되던 하네스 오류는 수정했으며, 같은 103초 테스트에서 정지 뒤
  요약과 로그 복사가 나타나고 자동 재시작이 발생하지 않는 것을 확인했다.
- 한국어 온디바이스 팩을 실제로 `install()`해서 인식 품질·지연을 재는 것(현재는 "내려받을
  수 있다"까지만 확인).
- iOS `SpeechAnalyzer`의 실제 한국어 정확도·지연, 서버 폴백 여부.

### 온디바이스 STT 실재 여부 (2026-09-05 확인)

**"미국 빅테크 API를 쓰면 온디바이스가 깨지는 것 아닌가"에 대한 답: 깨지지 않는다.**
구분해야 할 것은 *만든 회사*가 아니라 *오디오가 기기를 떠나는가*다.

- **iOS `SpeechAnalyzer`/`SpeechTranscriber`(iOS 26+)**: 전사 경로에 네트워크 호출이 없고,
  모델을 기기에 내려받아 쓴다. **출시 시점 지원 로케일에 한국어 포함.** 무료.
- ⚠ **구형 `SFSpeechRecognizer`는 기본이 서버 전송**이다. `requiresOnDeviceRecognition = true`를
  켜야 하고, 해당 로케일 모델이 없으면 **조용히 서버로 넘어간다.** 문서가 아니라 실측으로
  확인할 것(3-state: 온디바이스 / 서버 폴백 / 미지원을 뭉개지 말 것).
- **Android**: 온디바이스 인식이 존재하나 **기기·언어팩에 따라 서드파티 앱에 노출되지 않는
  빌드가 있다.** iOS만큼 균일하지 않다.
- **WhisperKit(MIT, CoreML)**: 대안. 기기에서 돌릴 만한 작은 모델은 한국어 정확도가 떨어지고
  실시간 지연이 관건이라 실측 없이 채택하지 않는다.

## 끝난 것

- [x] 해외 상용 프로바이더 실시간 지원 실태 조사 (Muse / Deepgram / Gemini Live / ElevenLabs)
- [x] 한국어 3화자 합성 픽스처 제작 (정답 전사·화자·경계 확정)
- [x] Muse·Deepgram 실시간 WebSocket 실호출 지연·정확도·화자분리 측정
- [x] 턴 간격 400ms / 900ms 대조 실험, Muse 배치 대조군
- [x] 실제 다화자 회의 녹음 5분으로 재검증
- [x] speech-toolkit에서 분리
- [x] 국내 실시간 STT 프로바이더 조사 → `docs/research/2026-09-03-korean-stt-providers.md`
- [x] 국제 오픈소스 실시간 캡션 조사 → `docs/research/2026-09-03-oss-live-caption.md`
- [x] 국내 실시간 문자통역 시장 조사 → `docs/research/2026-09-03-korean-caption-market.md`

## 확정된 결정

1. speech-toolkit과 별도 프로젝트로 간다.
2. 화자 라벨을 화면 구조의 기둥으로 삼지 않는다. 화자 전환 신호만 쓴다.
   (온라인 화자분리 엔진으로 정확도가 확보되면 재검토)
3. Gemini 3.5 Transcribe Live는 회의용 후보에서 제외(세션 10분 상한).
4. **온디바이스 범위는 하이브리드**(2026-09-05). 무료 온디바이스가 기본 경로이고,
   사용자 본인 키를 넣으면 고급 엔진으로 올라간다. 서버는 두지 않는다.
5. **지원 플랫폼은 iOS 네이티브 + 웹앱(모바일 지원)**(2026-09-05). 안드로이드 네이티브는
   장기 과제. 단 아이폰 웹은 온디바이스가 아니므로 두 경로의 약속이 다르다(위 표).
6. **첫 프로토타입은 웹앱**(2026-09-06). 판정 기준은 검증 속도(URL 공유)이며, 온디바이스
   축은 프로토타입에서 검증하지 않는다.

## 뒤집힌 결정

- ~~프로바이더 1순위는 Meta Muse~~ → **폐기.** 무료 배포가 전제로 확정되면서 종량 과금
  클라우드 API는 구조적으로 부적합해졌다. 온디바이스가 기본선이다.
- ~~씨사운드는 화자 분리를 지원하지 않는 1:1 대면용 하드웨어~~ → **틀렸다**(2026-09-05).
  앱 단독으로 동작하고 8인 회의에서 실시간 화자 분리가 실용 수준으로 작동한다.
  홍보 문구에 없다는 것을 기능이 없다는 근거로 삼은 오류였다.
- ~~온디바이스의 근거는 프라이버시~~ → **비용이 1차 근거이고 프라이버시는 부수 효과.**
- ~~실시간 화자 분리는 스트리밍의 구조적 한계~~ → **과잉 일반화였다.** 상용 API 구현의
  문제이고, diart·NVIDIA Streaming Sortformer 같은 온라인 전용 알고리즘은 실제로 동작한다.
  (speech-toolkit 공개 문서에도 정정 반영해 푸시함)

## 미결 결정 (다음 논의 대상)

- **이 프로젝트를 계속할 것인가.** 위 「전제 재검토」의 세 축이 당사자에게 실제로 의미 있는지
  확인되지 않으면 만들 이유가 약하다.
- **당사자 검증을 어떻게 할 것인가.** 조사가 짚은 빈 곳은 전부 검색 기반 추론이다. 특히
  "프라이버시 우려"는 조사자가 추정한 것이지 당사자가 말한 것이 아니다.
- **화자 전환을 화면에 어떻게 표시할 것인가.** 라벨 없이 문단만 나눌지, 색만 바꿀지.

## 남은 검증 과제

- WhisperLiveKit + 한국어 파인튜닝 Whisper 조합의 실제 지연·정확도 미측정
- Streaming Sortformer / diart의 한국어 화자분리 정확도 미측정(같은 픽스처로 상용 API와 비교 가능)
- **Apple SFSpeechRecognizer 한국어 실시간 품질 미측정** (프라이버시 축을 택하면 이것이 1순위)
- **청각장애인 발화 인식률**은 어떤 엔진으로도 측정한 적이 없다. 검증하려면 당사자 음성 필요
- sherpa-onnx 한국어 스트리밍 모델의 빈 결과 버그 재현 여부
- 소보로 플러스 직접 사용 평가(경쟁자 실물 확인)
- 실녹음 검증이 5분 1건뿐. 잡음·거리·겹침 조건 확대 필요
