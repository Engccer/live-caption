# 변경 이력

시점 기록이다. 지나간 항목은 그때 참이었던 그대로 두고 최신화하지 않는다.
지금 참인 상태는 `PROGRESS.md`, 아직 하지 않은 것은 `docs/BACKLOG.md`에 있다.

## 2026-09-13

- **말동무 웹앱 1차 설계 확정.** 브라우저 내장 Web Speech API를 쓰는 정적 파일 넷으로 한 화면짜리
  자막 앱을 만들기로 했다. 앱 이름은 말동무, 사용 상황은 교무회의·연수, 기록은 남기지 않는다.
  전문은 `docs/superpowers/specs/2026-09-13-maldongmu-web-v1-design.md`.
- **웹에는 화자 전환 신호가 없다는 전제를 못 박았다.** 스파이크의 "화자 전환은 쓸 만하다"는 결론은
  Muse·Deepgram의 `speaker` 이벤트를 전제한 것이고, 브라우저 내장 API는 그 정보를 주지 않는다.
  화면에 그리는 단위는 화자가 아니라 확정 결과 하나짜리 발화 덩어리로 정했다.
- **고지 문구를 기기 무관 단일 문안으로 확정.** 아이폰만 골라 경고하면 노트북으로 여는 사람에게
  잘못된 안심을 주고(데스크톱 크롬도 팩을 내려받기 전에는 서버로 보낸다), 판별 API가 없는
  브라우저에서는 표시가 실제 동작과 어긋난다. 어긋났을 때의 대가가 큰 쪽을 피했다.
- **병렬 구현 계획 작성.** 인식 수명주기·화면·문서 세 세션으로 갈라 파일 소유권이 겹치지 않게 나눴다.
- **필수 문서를 갖췄다.** `README.md`·`CHANGELOG.md`·`docs/BACKLOG.md`를 새로 만들고 `PROGRESS.md`에서
  미결 결정·검증 과제·뒤집힌 결정을 백로그로, 완료 기록을 이 파일로 옮겼다.

## 2026-09-10

- **모바일 Safari 사전 게이트를 조건부 통과로 판정.** 통제된 `abort()` 뒤 5초 대기 재시작 실험에서
  첫 확정 결과 뒤 17.3초에 인식이 끝나고 22.3초에 새 오디오 세션이 시작됐으며, 25.2초에 발화를
  감지해 28.8초에 두 번째 확정 결과를 냈다. 재시작 전후 전사를 모두 보존했고 오류는 없었다.
  정상 종료된 인식의 앱 수준 재연결은 통과한 것이다.
- **장시간 인식의 간헐성 기록.** 같은 기기에서 487초를 재면 약 40.2초 간격으로 12번 끊겼고 첫
  재시작 뒤에는 한 발화도 감지하지 못했다. `audiostart`가 왔다는 것만으로 마이크 입력이 복구됐다고
  판정할 수 없다는 뜻이다. 반면 새로 연 페이지에서는 첫 세션이 163초간 끊기지 않았고 156초 뒤
  시작한 발화를 확정 결과로 반환했다. 40초 종료는 침묵에 따른 고정 상한이 아니라 WebKit 오디오
  캡처 상태에 달린 간헐적 실패다.
- **iPhone Safari 장시간 인식 실측.** iPhone 13 Pro / Safari 26.6.1에서 103초 동안 두 번 끊겼고
  `audio-capture: Source is stopped`와 `aborted: No speech detected`가 함께 나왔다. 즉시 재시작은
  각각 4ms와 2ms 안에 `audiostart`까지 갔다.
- **스파이크 하네스 수정.** 사용자가 정지한 뒤에도 새 세션이 시작되던 오류를 고치고, 종료 이벤트
  뒤에 요약이 생성되도록 했다. 재시작 전에는 오디오 정리를 기다린다.

## 2026-09-06

- **첫 프로토타입은 웹앱으로 확정.** 판정 기준을 "무엇을 먼저 만들까"가 아니라 "무엇을 먼저
  검증할까"로 바꾼 결과다. 이 프로젝트에서 판정을 여는 재료는 전부 당사자 증언이었고, 공유
  가능한 URL이 그 재료를 가장 빨리 만든다. TestFlight는 설치·초대·심사가 끼어 루프를 늦춘다.
- **모바일 Safari 연속 인식 측정 페이지 추가**(`spike/web/`). 아이폰에서 실제로 얼마나 오래
  끊기지 않는지를 재기 위한 하네스다.
- **티로(Tiro) 조사 반영.** 청각장애 당사자가 소보로보다 정확하다고 평가하는 AI 회의록 앱이며,
  실시간 중에는 화자를 가르지 않고 기록 종료 후 전체를 재분석한다. 상용 API 실측 결론이 제품
  차원에서 재확인됐다.

## 2026-09-05

- **프로젝트 전제 확정: 무료·오픈소스.** 상용 제품과 기능으로 경쟁하지 않고 진입 장벽을 없애는
  것을 목표로 삼았다. 성공 판정은 계정·결제 없이 첫 캡션까지 도달하는가, 사용자가 한 푼도 내지
  않는가, 다른 개발자가 포크할 수 있는가 셋이다.
- **온디바이스의 근거를 프라이버시에서 비용으로 바꿨다.** 라이브 캡션은 오디오를 연속으로
  흘려보내는 가장 비싼 사용 형태라 무료 배포와 클라우드 STT가 산술적으로 양립하지 않는다.
  프라이버시는 그렇게 택한 구조의 부수 효과다.
- **온디바이스 범위를 하이브리드로 확정.** 기본은 온디바이스 무료, 고급 엔진은 사용자 본인 키로
  선택하며 사용료는 사용자가 사업자에게 직접 낸다. 서버는 두지 않는다.
- **지원 플랫폼을 iOS 네이티브 + 웹앱으로 확정**하고 Chrome 152(macOS)에서 웹 온디바이스 가용성을
  실측했다. `ko-KR` + `processLocally:true`가 `downloadable`을 돌려주는 반면 iOS Safari는 오디오를
  애플 서버로 보낸다. 아이폰 웹앱은 온디바이스가 아니다.
- **씨사운드 조사 오류 정정.** "화자 분리를 지원하지 않는 1:1 대면용 하드웨어"라는 서술은 틀렸다.
  앱 단독으로 동작하고 8인 회의에서 실시간 화자 분리가 실용 수준으로 작동한다. 홍보 문구에 없다는
  것을 기능이 없다는 근거로 삼은 오류였다.

## 2026-09-04

- **씨사운드 실측 조사 반영.** 기기 가격과 인앱 구독료, 오디오의 클라우드 전송 여부를 확인했다.
  (이때 함께 적은 "화자분리 부재"는 다음 날 오류로 판명돼 정정했다.)

## 2026-09-03

- **프로젝트 착수.** speech-toolkit에서 실시간 스트리밍을 떼어 별도 저장소로 분리했다. 입출력
  계약·의존성·트리거 표면이 모두 다르고, 목표가 CLI 스크립트가 아니라 청각장애인용 앱이라는 것이
  분명해졌기 때문이다.
- **해외 상용 프로바이더 실시간 지원 실태 조사**(Muse / Deepgram / Gemini Live / ElevenLabs)와
  **실호출 측정.** 한국어 3화자 합성 픽스처를 만들어 정답 전사·화자·경계를 확정하고, 턴 간격
  400ms와 900ms를 대조하며 Muse 배치 대조군까지 쟀다. Muse의 partial 지연 중앙값은 151ms,
  Deepgram은 317ms에 최대 2.3초까지 튀었다. 전문은 `docs/spike-2026-09-03-realtime-stt.md`.
- **실제 다화자 회의 녹음 5분으로 재검증.** 상용 API의 실시간 화자 분리가 스트리밍 3명 < 배치 4명
  < 상용 배치 전사 7명으로 과소 계수됐다. 합성 음원의 균일한 400ms 간격은 엔드포인팅을 방해해
  실제보다 나쁜 결과를 낸다는 것도 이때 확인했다.
- **국내 프로바이더·국제 오픈소스·국내 시장 조사 3건**(`docs/research/`). "실시간 화자 분리는
  스트리밍의 구조적 한계"라는 결론을 정정했다. diart와 NVIDIA Streaming Sortformer는 처음부터
  온라인으로 설계된 알고리즘이고 실제로 동작한다. 상용 API 구현의 문제였다.
