__pycache__/
*.pyc
.env
.env*.local
.DS_Store

# 픽스처 중간 산출물 (make_fixture.py로 재생성)
spike/turns/
spike/_concat.txt

# 실제 녹음과 그 파생물은 절대 커밋하지 않는다 (CLAUDE.md 테스트 음원 취급 규칙)
spike/real/
*.m4a
*_전사*.txt

# Vercel CLI 링크 메타데이터 (spike 배포용)
.vercel/
docs/private/
.playwright-mcp/

# 실녹음 eval 자료 (발화 내용 포함, CLAUDE.md 테스트 음원 취급 규칙)
# MANIFEST.md만 커밋한다 - 집계 수치와 판정만 담고 발화 내용은 담지 않는다.
eval/**/audio/
eval/**/ref/
eval/**/baseline/
