__pycache__/
*.pyc
.env
.env*.local
.DS_Store

# 픽스처 중간 산출물 (make_fixture.py로 재생성)
spike/turns/
spike/_concat.txt

# 실제 녹음과 그 파생물은 절대 커밋하지 않는다 (CLAUDE.md 테스트 음원 취급 규칙)
spike/real/
*.m4a
*_전사*.txt

# Vercel CLI 링크 메타데이터 (spike 배포용)
.vercel/
docs/private/
.playwright-mcp/

# 실녹음 eval 자료는 저장소 밖에 둔다($LIVE_CAPTION_EVAL_DATA, 기본 ~/live-caption-eval).
# 아래는 실수로 저장소 안에 만들었을 때를 위한 안전망이다. eval/ 아래 커밋되는 것은
# MANIFEST.md(집계 수치와 판정만)와 tools/(발화 내용 없음)뿐이다.
eval/**/audio/
eval/**/ref/
eval/**/baseline/
