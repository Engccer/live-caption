> 🤖 **이 파일은 자동 생성됩니다. 직접 수정하지 마세요.**
> 정본은 `CLAUDE.md` 입니다. 내용을 바꾸려면 `CLAUDE.md` 를 수정한 뒤
> 프로젝트 루트에서 `python sync_agent_docs.py` 를 실행하세요.
> 이 파일을 직접 고치면 다음 동기화 때 경고와 함께 덮어쓰기 대상이 됩니다.

<!-- SYNC-BODY-START: 이 줄 아래 본문은 CLAUDE.md 와 100% 동일하게 자동 생성됨 -->
# live-caption

> 접근성 정본은 글로벌 헌장이다. 아래 import로 이 프로젝트의 매 세션에 로드된다(화면이 있는 프로젝트의 CLAUDE.md에만 두는 규약).
@~/.claude/ACCESSIBILITY.md

이 파일은 Claude Code(claude.ai/code)가 이 저장소에서 작업할 때 참고하는 가이드다.

## 무엇을 만드는가

**앱 이름은 말동무다.** 청각장애인 사용자를 위한 **실시간 문자 통역(라이브 캡션) 웹앱 +
iOS 앱**이다. 마이크로 주변 대화를 받아 화면에 실시간 텍스트로 띄운다. 회의·수업·상담처럼
여러 사람이 말하는 자리에서 당사자가 대화를 눈으로 따라갈 수 있게 하는 것이 목적이다.

**이 앱에는 요청자가 있다.** 2026년 교육부 장애인교원 교육전념 여건 지원 사업의
「업무 자동화 개발 연구」 전학공이 교사들에게 필요한 프로그램 제작을 신청받았고, 그중
청각장애 교사 한 분이 **"회의나 연수 시에 음성을 자막으로 실시간 변환"**을 요청한 것이
출발점이다. 상상한 사용자가 아니라 실제 사람의 요구다.
개인 식별 정보가 포함된 배경 기록은 `docs/private/background.md`에 있다(gitignore).

**지금은 웹앱 1차본을 만드는 단계다.** 조사는 끝났다. 코드는 `web/`(1차 웹앱),
`spike/`(스파이크 하네스), `eval/`(엔진 판정 도구와 자료 기록) 셋이고, 화면·상태·인터페이스
계약은 `docs/superpowers/specs/2026-09-13-maldongmu-web-v1-design.md`가 정본이다.
iOS 앱은 그다음이다.

## 문서 역할

한 사실은 한 자리에만 둔다. 두 문서가 같은 문장을 들면 한쪽이 반드시 낡는다.

| 문서 | 무엇을 담나 |
|---|---|
| `CLAUDE.md` | 항구 규칙과 함정 (이 파일) |
| `README.md` | 사람 독자용 소개와 실행법 |
| `PROGRESS.md` | 지금 참인 상태, 다음 단계, 알려진 한계 |
| `docs/BACKLOG.md` | 미결 결정, 남은 검증 과제, 뒤집힌 결정 |
| `CHANGELOG.md` | 날짜별 변경. 시점 기록이라 최신화하지 않는다 |

## 사용자가 둘이라는 점을 잊지 말 것

이 프로젝트에는 성격이 다른 두 접근성 요구가 동시에 걸린다.

- **앱의 최종 사용자는 청각장애인**이다. 화면을 눈으로 읽으므로 가독성이 핵심이다:
  큰 글씨, 높은 대비, 읽던 자리를 잃지 않는 안정적인 스크롤, 확정 텍스트와 중간 결과의
  시각적 구분. 화면낭독기 낭독 순서는 이 사용자에게 핵심 관심사가 아니다.
- **개발자는 시각장애인 화면낭독기 사용자**다. 개발 도구·CLI·테스트 하네스의 출력은
  글로벌 접근성 헌장(`~/.claude/ACCESSIBILITY.md`)을 그대로 따른다.
- 두 요구는 충돌하지 않지만 **적용 대상이 다르다.** 캡션 표시 영역에 화면낭독기용 live region을
  거는 것은 과잉이며(§2 과잉 live region), 반대로 설정 화면·컨트롤은 헌장을 100% 지킨다.

## 실측으로 확정된 사실

전문은 `docs/spike-2026-09-03-realtime-stt.md`. 재현 코드는 `spike/`.

1. **해외 상용 API 중에서는 Meta Muse Voice Transcribe가 실시간 전사 1순위다.** partial 지연
   중앙값 151ms, 최대 208ms로 흔들림이 없다. Deepgram은 중앙값 317ms에 최대 2.3초까지 튄다.
2. **상용 API의 실시간 화자 분리는 신뢰할 수 없다.** 실제 회의 녹음에서 스트리밍 3명 <
   배치 4명 < 상용 배치 전사 7명으로 과소 계수됐다. **단 이것은 배치 전제 화자분리를
   스트리밍에 얹은 구현의 문제이지 원리적 한계가 아니다**(아래 오픈소스 항목 참조).
   ⚠ **이것을 "한국어 실시간 화자분리는 어렵다"로 일반화하지 말 것.** 씨사운드가 8인 회의에서
   실용 수준의 실시간 화자 분리를 낸다(2026-09-05 실사용 확인). 상한이 아니라 그 두 API의 한계다.
   ⚠ **배치에서도 무너진다.** 클로바노트가 한 회의를 둘로 나눠 녹음한 파일에 화자 번호를
   따로 매겨 같은 번호가 서로 다른 사람을 가리켰고, 발화가 1~5회로 짧은 참석자 4명은
   특정하지 못했다(2026-09-14, 중집위 16차 전사본에 기록된 실측).
   ⚠ **상용 전사의 화자 라벨을 정답으로 쓰지 말 것.** 사람이 번호를 실명으로 고쳐 놓았더라도
   바뀐 것은 이름표뿐이고 **화자 경계를 그은 것은 여전히 그 API**다. 상용을 재는 기준이
   상용이면 순환이다. 전사 텍스트도 같은 이유로 WER 정답이 될 수 없다.
3. **"화자가 바뀌었다"는 신호는 쓸 만하다.** 실녹음 5분에서 세그먼트 52개 **모두에** 화자
   전환 이벤트가 붙었다(52개에 52회). 정체(who)는 못 믿어도 전환(when)은 쓸 수 있다.
   ⚠ **단 이건 Muse·Deepgram의 `speaker` 이벤트를 받을 때 이야기다. 브라우저 내장 Web Speech
   API에는 화자 정보가 아예 없어 웹앱에는 이 신호 자체가 없다.**
4. **Gemini 3.5 Transcribe Live는 회의용에서 제외한다.** 실시간 화자 분리 공식 미지원 +
   세션 10분 상한.
5. **합성 음원으로 턴 분절을 판단하지 말 것.** 균일한 400ms 간격은 엔드포인팅을 방해해
   실제보다 훨씬 나쁜 결과를 낸다. 실녹음에서는 정상 작동했다.

## 조사로 확인된 선택지

상세는 `docs/research/`.

- **온라인 화자 분리는 오픈소스로 실제 된다.** `diart`(MIT, 롤링버퍼 증분 클러스터링)와
  **NVIDIA Streaming Sortformer**(Apache-2.0, 화자 캐시, 지연 0.08~1.12초 설정 가능)는
  처음부터 온라인으로 설계된 알고리즘이다. 상용 API 실측 실패를 일반화하지 말 것.
  주의: `sherpa-onnx`의 화자분리는 이름과 달리 **오프라인 후처리**다.
- **[WhisperLiveKit](https://github.com/QuentinFuxa/WhisperLiveKit)**(Apache-2.0)은
  AlignAtt/LocalAgreement 저지연 정책 + Streaming Sortformer 화자분리 + 번역이 이미 조립된
  셀프호스팅 서버다. ⚠ **1차본은 쓰지 않는다.** 서버를 두지 않기로 확정했고 웹앱은 브라우저
  내장 API로 간다. 포크해서 셀프호스팅하는 사람의 경로다.
- **웹은 실행 환경에 따라 온디바이스 여부가 갈린다**(2026-09-05 Chrome 152 실측).
  데스크톱 Chrome은 `SpeechRecognition.available({langs:['ko-KR'], processLocally:true})`가
  `downloadable`을 돌려준다(한국어 온디바이스 가능). **그러나 iOS Safari는 오디오를 애플
  서버로 보낸다** — iOS의 모든 브라우저가 WebKit이므로 아이폰 웹에서는 온디바이스가
  성립하지 않는다. ⚠ **데스크톱 크롬도 한국어 팩을 내려받기 전까지는 서버로 보낸다.**
  그래서 화면 고지는 기기를 가리지 않는 단일 문안을 쓰고, "기기 안에서만 처리"는 어느
  기기에서도 표기하지 않는다.
- **iOS 단계에서 검토할 후보는 애플 음성 인식 API**다(구형 `SFSpeechRecognizer`,
  iOS 26+ `SpeechAnalyzer`/`SpeechTranscriber`). 무료 온디바이스 한국어 실시간 경로로
  문서화돼 있으나 **아직 하나도 실측하지 않았다** — 이 저장소에 iOS 코드가 없다.
  부족하면 WhisperKit(MIT, CoreML)으로 교체. 측정 과제는 `docs/BACKLOG.md`.
- **티로(Tiro)는 청각장애 당사자가 소보로보다 정확하다고 평가하는 AI 회의록 앱이다**
  (2026-09-06 조사). ⚠ 지인 제보의 "타로"는 점술 앱이 아니라 이 앱이며, 검색 시 이름이 겹친다.
  티로도 **실시간 중에는 화자를 가르지 않고 기록 종료 후 전체를 재분석한다** — 상용 API 실측
  결론이 제품 차원에서 재확인된 것이다. "1대1에서 정확도가 매우 좋다"는 제보는 1대1이
  화자 분리가 필요 없는 유일한 구성이기 때문이다.
- **국내 프로바이더는 실시간 스트리밍은 되지만 실시간 화자분리는 전부 없다.** 리턴제로
  (시간당 1,000원, 무료 600분)가 문서·가격 투명성 최고. 국내 리전·개인정보 국내 보관이
  필요할 때의 선택지.

## Muse 실시간 WebSocket 함정

- **인증은 핸드셰이크 첫 JSON 프레임으로 보낸다. `Authorization` 헤더는 무시된다.**
- 오디오는 컨테이너 없는 raw PCM(s16le mono) 바이너리 프레임, 80ms 청크, 실시간 페이싱.
  **5초 이상 앞질러 보내면 안 된다.**
- `speaker` 이벤트는 **그 뒤가 아니라 앞의 오디오**에 라벨을 붙이며 발화 경계를 뜻하지 않는다.
- 종료는 `{"type":"endStream"}` 후 close code 1000까지 수신 계속.

## 웹앱 1차 규칙

- **의존성을 늘리지 않는다.** 빌드 도구도 외부 패키지도 없다. 브라우저 내장 Web Speech API와
  `node:test`만 쓴다. 포크하는 사람이 `npm install` 없이 파일을 열 수 있어야 한다.
- **테스트는 `web/`에서 `node --test`.** ⚠ `node --test test/`처럼 **디렉터리를 인자로 주면
  Node 26이 그것을 모듈로 해석해 `MODULE_NOT_FOUND`로 죽는다.** 테스트가 실패한 것처럼 보이지만
  테스트는 돌지도 않은 것이다. 인자 없이 쓰거나 파일을 직접 지정한다.
- **브라우저에서는 `timers`를 주입하지 않는다.** `{ setTimeout, clearTimeout }`를 평범한 객체에
  담아 넘기면 `TypeError: Illegal invocation`으로 죽는다(브라우저의 `setTimeout`은 호출할 때
  `this`가 진짜 `Window`인지 검사한다). ⚠ **Node에는 그 검사가 없어 `node --test`는 전부
  통과하고**, 피해가 재시작 예약에서만 나므로 아이폰에서 40초 뒤 첫 끊김이 왔을 때 자막이
  아무 오류도 없이 조용히 멈춘다. 주입은 테스트에서만 한다.
- **live region은 `#status` 하나뿐이다.** 자막 영역에는 걸지 않는다(위 「사용자가 둘」).
- **UI 문구에 이모지와 em dash를 쓰지 않는다.** 아이콘도 1차에는 두지 않는다.
- **화면 고지는 기기를 가리지 않고 항상 같은 문구**를 띄운다. "기기 안에서만 처리"라고 쓰지 않는다.

## 테스트 음원 취급 규칙

- `spike/`의 합성 픽스처는 Gemini TTS로 만든 가상 대화이므로 자유롭게 커밋·공개 가능하다.
- **실제 회의 녹음과 그 파생물(전사 텍스트, 결과 JSON)은 이 저장소에 넣지 않는다.**
  남길 수 있는 것은 **발화가 복원되지 않는 집계값과 측정 조건**뿐이다(화자 수, 턴 수,
  오류율, 음향 통계, 분당 글자수 따위).
- 새 실녹음으로 검증할 때도 같은 규칙을 지킨다. 발화 내용은 어떤 산출물에도 남기지 않는다.
- **"넣지 않는다"는 커밋만이 아니라 작업 디렉터리까지다.** 이 저장소는 공개돼 있고 포크를
  전제로 만들었다. gitignore로 가린 채 작업 디렉터리에 두면 규칙 한 줄이 바뀌는 순간
  공개된다. 자료는 `$LIVE_CAPTION_EVAL_DATA`(기본 `~/live-caption-eval`) 아래 두고
  도구에 경로를 넘긴다. ⚠ 2026-09-14에 이것을 어겨 `eval/*/audio|ref|baseline`에 넣었다가
  같은 날 되돌렸다. gitignore는 안전망으로 남겨 뒀을 뿐 그것이 규칙을 만족시키지 않는다.
- **저장소에 남는 eval 산출물은 `eval/<세트>/MANIFEST.md`와 `eval/tools/`뿐이다.**
  MANIFEST는 집계 수치·측정 조건·판정만 담고 발화를 인용하지 않는다.
- ⚠ **개인정보는 발화 내용만이 아니다.** 참석자 실명, 누가 얼마나 말했는지, 조직 내부
  드라이브의 폴더 경로도 개인정보이며 **공개 저장소에 커밋하지 않는다.** MANIFEST의 화자는
  A·B·C로 쓰고 실명 매핑은 `<자료>/ref/speakers.md`에, 원본 경로는 `<자료>/SOURCES.md`에
  둔다(둘 다 저장소 밖). ⚠ 2026-09-14에 실명 6명과 발언 비중, 내부 경로를 MANIFEST에
  커밋했다가 같은 날 지웠다. **커밋 이력에는 남아 있다.**
- **eval 정답 자료는 사람이 작성한 것만 쓴다.** 상용 API 출력을 정답으로 삼으면 상용을
  재는 기준이 상용이 되어 순환이다. 화자 전환 정답도 **경계를 사람이 그은 것**이어야 한다
  (사람이 화자 번호를 실명으로 고쳐 놓았더라도 경계를 그은 것은 그 API다).
  이 기준 하나로 중집위 회의 20시간이 탈락했다. 판정은 `eval/*/MANIFEST.md`.

## 관련 저장소

- [speech-toolkit](https://github.com/Engccer/speech-toolkit): 배치 TTS·STT CLI 모음.
  이 프로젝트가 다루는 **실시간 스트리밍은 speech-toolkit의 범위가 아니다**(입출력 계약과
  의존성이 다름). 빌려 쓰는 자리는 둘이고 **양쪽 다 `SPEECH_TOOLKIT` 환경변수로 위치를
  지정한다**: `spike/make_fixture.py`가 픽스처 합성에 `gemini_tts.py`를, eval이 정답 정렬용
  배치 전사에 `STT/`를 쓴다.
